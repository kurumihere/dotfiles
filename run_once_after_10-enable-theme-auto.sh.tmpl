#!/usr/bin/env bash
set -euo pipefail

if command -v systemctl >/dev/null 2>&1; then
  systemctl --user daemon-reload || true
  systemctl --user disable --now theme-light.timer theme-dark.timer >/dev/null 2>&1 || true
  systemctl --user enable --now theme-auto.timer >/dev/null 2>&1 || true
fi
